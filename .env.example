# Google Apps Script Web App URL — direct POST from browser (no server route)
# Contact Form: sheet row append + resume Drive save + email notification
NEXT_PUBLIC_APPS_SCRIPT_URL=

# Google Apps Script Web App URL — Referral Form (separate sheet)
NEXT_PUBLIC_REFERRAL_APPS_SCRIPT_URL=

# Google Analytics & GTM (optional — renders nothing when absent)
NEXT_PUBLIC_GA_MEASUREMENT_ID=
NEXT_PUBLIC_GTM_ID=
